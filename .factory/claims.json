[
  {
    "id": "demo-isolated",
    "claim": "Try the sample project without saving it to your real runbooks.",
    "where": "/demo, landing primary action, README",
    "test": "npm run test:e2e -- --grep @claim:demo-isolated",
    "sandbox": "fresh browser context; run sample; assert only the demo session namespace is used and reset removes it"
  },
  {
    "id": "exact-environment-review",
    "claim": "Review the exact executable, arguments, only child environment, working folder, sandbox status, and rollback note before consent.",
    "where": "/demo, landing safety copy, desktop review, README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_exact_environment_review",
    "sandbox": "temporary native runbook without cwd; prepare its real native plan; assert the canonical YAML folder, then set HOTKEY_QA8_INHERITED_MARKER in the parent and prove /usr/bin/printenv receives neither it nor any unreviewed value while a reviewed value is present"
  },
  {
    "id": "platform-sandbox-boundary",
    "claim": "When Linux permits Landlock, file changes by a runbook are limited to its reviewed working folder; other platforms show that no process sandbox is active.",
    "where": "landing safety details, desktop consent, README, terms",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_platform_sandbox_boundary",
    "sandbox": "Linux kernel with Landlock enabled: a fixed direct child command writes once inside and once outside the reviewed folder; assert the inside write succeeds and the outside write is denied. If the platform does not permit Landlock, assert the reviewed status is unavailable."
  },
  {
    "id": "demo-privacy",
    "claim": "The browser sample sends no data to third parties.",
    "where": "/demo banner, README, .factory/demo.md",
    "test": "npm run test:e2e -- --grep @claim:demo-privacy",
    "sandbox": "fresh browser context; complete sample flow; record every request and assert all are same-origin"
  },
  {
    "id": "native-demo-controls",
    "claim": "The installed sample has a persistent demo banner with Reset demo and Start for real controls.",
    "where": "desktop sample, README, .factory/demo.md",
    "test": "npm run test:unit -- --testNamePattern @claim:native-demo-controls",
    "sandbox": "render the native demo-mode UI from active and inactive state; assert the banner, isolation notice, reset action, and exit action"
  },
  {
    "id": "native-safety-contract",
    "claim": "Reviewed folders reject symlinks and world-writable roots, enforce three-level, 64 KB, 100-file, and 20-step limits, and use content digests. Programs stay fixed; text, integer, choice, boolean, path, and secret values use direct arguments and environment entries; patterns validate; secrets are masked and redacted; rollback notes remain attached.",
    "where": "landing safety and method sections, desktop app, README safety model",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_native_safety_contract",
    "sandbox": "temporary owned runbook directories; reject symlink and world-writable roots; enforce depth, file, folder, and step limits; validate every parameter type and an author pattern; reject a parameterized program; prepare direct argv and environment; mask and redact a secret; retain rollback; edit YAML and assert the digest changes"
  },
  {
    "id": "local-privacy",
    "claim": "Runbooks, trust records, parameters, output, and history stay on the device; only release metadata and explicit license checks use documented services.",
    "where": "landing facts, desktop settings, privacy page, README",
    "test": "npm run test:unit -- --testNamePattern @claim:local-privacy",
    "sandbox": "inspect every runtime source for network origins and assert demo and real history use separate local files"
  },
  {
    "id": "no-account",
    "claim": "The free sample and free desktop tier need no account.",
    "where": "landing license section, desktop Settings, README",
    "test": "npm run test:e2e -- --grep @claim:no-account",
    "sandbox": "fresh browser context; complete the free sample without a sign-in, login, or account-creation step"
  },
  {
    "id": "no-telemetry",
    "claim": "Hotkey Runbook includes no telemetry or analytics.",
    "where": "desktop Settings, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:no-telemetry",
    "sandbox": "fresh browser context; record the landing and complete demo request flow; allow only same-origin assets and the documented GitHub release-metadata request"
  },
  {
    "id": "no-cloud-sync",
    "claim": "The desktop app does not include cloud sync.",
    "where": "desktop Settings, README",
    "test": "npm run test:unit -- --testNamePattern @claim:no-cloud-sync",
    "sandbox": "inspect native dependencies and bridge commands; assert there is no HTTP client, sync command, remote-storage plugin, or remote orchestration command"
  },
  {
    "id": "free-tier-limits",
    "claim": "The free app shows 3 runbooks and 10 history entries; a valid license shows up to the 100-entry local history limit.",
    "where": "landing license section, desktop settings, README, terms",
    "test": "npm run test:unit -- --testNamePattern @claim:free-tier-limits",
    "sandbox": "100 deterministic items; assert free runbook and history slices, then assert the licensed view contains all 100"
  },
  {
    "id": "licensed-runbooks",
    "claim": "A valid existing license removes the product's three-runbook limit: every locally available runbook is shown.",
    "where": "landing license section, desktop Settings, README, terms",
    "test": "npm run test:unit -- --testNamePattern @claim:licensed-runbooks",
    "sandbox": "101 deterministic reviewed runbooks; assert the free view shows 3 and the licensed view returns every runbook without a product count cap"
  },
  {
    "id": "installer-integrity",
    "claim": "Release installers include a SHA-256 checksum file whose names match the downloadable assets.",
    "where": "landing checksum link, release workflow, README",
    "test": "npm run test:unit -- --testNamePattern @claim:installer-integrity",
    "sandbox": "temporary cross-platform artifact tree with Tauri space-containing names; generate release assets and run sha256sum -c"
  },
  {
    "id": "keyboard-first-desktop",
    "claim": "Use the desktop app by keyboard: focus the filter, choose a runbook, prepare it, and confirm the reviewed run.",
    "where": "README, desktop app keyboard hints",
    "test": "npm run test:unit -- --testNamePattern @claim:keyboard-first-desktop",
    "sandbox": "test the desktop keyboard controller for Ctrl/Command+K, wrapped arrow selection, Enter parameter focus, and Ctrl/Command+Enter review and confirmation shortcuts"
  },
  {
    "id": "installer-sh-checksum",
    "claim": "The macOS/Linux installer verifies SHA-256 before placing an app on PATH.",
    "where": "landing download section, README, public/install.sh",
    "test": "npm run test:unit -- --testNamePattern @claim:installer-sh-checksum",
    "sandbox": "temporary HOME and mocked release download; run the shipped installer with matching bytes, then a mismatched checksum and assert nothing is installed"
  },
  {
    "id": "installer-ps1-checksum",
    "claim": "The Windows installer compares SHA-256 before it can start the MSI.",
    "where": "landing download section, README, public/install.ps1",
    "test": "npm run test:unit -- --testNamePattern @claim:installer-ps1-checksum",
    "sandbox": "Windows release runner mocks the download and MSI launcher, then runs the shipped PowerShell script with matching and mismatched bytes; Unix test environments also assert the checksum guard precedes Start-Process"
  },
  {
    "id": "existing-license-recovery",
    "claim": "An existing license token can be restored on another device.",
    "where": "landing license section, desktop Settings, README, terms",
    "test": "npm run test:e2e -- --grep @claim:existing-license-recovery",
    "sandbox": "fresh browser context; intercept the documented verification endpoint with a valid fixture; restore a token, assert namespaced local storage, reload, and assert the daily verdict cache avoids a second request"
  },
  {
    "id": "one-time-license-purchase",
    "claim": "A $29 one-time license is purchased through the Sociobot-hosted checkout.",
    "where": "landing license section, desktop Settings, README, terms",
    "test": "npm run test:e2e -- --grep @claim:one-time-license-purchase",
    "sandbox": "fresh browser context; intercept only the documented scoped checkout endpoint, click the $29 purchase link, and assert navigation reaches the hosted checkout fixture"
  }
]
